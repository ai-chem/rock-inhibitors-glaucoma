# Analysis environment: ocular property classifiers, scoring and figures.
#
# Covers the notebooks in scripts/classifiers/ and loading the fitted models
# in models/. The generative models are NOT covered here: each upstream
# repository cloned by scripts/setup.sh has its own, mutually incompatible
# dependencies (notably conflicting torch versions). Create a separate
# environment per method as described in methods/*/README.
#
# Version pins matter for models/: scikit-learn and XGBoost pickles are not
# portable across releases, so the models will fail to load under different
# versions.
#
# Tested with Python 3.11.14.
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

numpy==2.4.1
pandas==3.0.0
rdkit==2025.9.4

scikit-learn==1.9.0
xgboost==3.2.0
joblib==1.5.3

matplotlib==3.10.8
seaborn==0.13.2
plotly==6.8.0
